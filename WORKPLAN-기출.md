# 기출 13회차 전사 — 여러 세션이 나눠서 하는 법

교재 **PART 04 과년도 + 최근 기출(복원)문제** 13회차, 약 750문항을 전사합니다.
세션 여러 개가 동시에 붙어도 겹치지 않도록 **회차 하나 = 파일 하나**로 나눠 두었습니다.

---

## 1. 회차 찜하기 — 파일이 곧 팻말입니다

`exam/` 안에 **아직 없는** 회차를 하나 고르고, 고르는 즉시 빈 파일을 만드세요.
이미 파일이 있으면 다른 세션이 하고 있는 것이니 건너뜁니다.

```bash
ls "C:\Users\user\Desktop\claude code\신재생에너지 마스터\exam"
```

```bash
python addq.py 2018-2 --new "2018년 제2회 과년도 기출복원문제"
```

파일이 이미 있으면 스크립트가 거부합니다. 그러면 다음 회차를 고르세요.

---

## 2. 회차별 쪽 지도

`pages.py` 의 쪽 인덱스는 **PDF 안에서 0부터 센 번호**입니다(교재 쪽번호가 아닙니다).

| 회차 | 파일명 | PDF | 쪽 인덱스 | 교재 쪽 | 문항 수 |
|---|---|---|---|---|---|
| 2016년 제2회 | `2016-2.js` | B | 0 ~ 20 | 349~369 | 60 ✅ |
| 2016년 제5회 | `2016-5.js` | B | 21 ~ 41 | 370~390 | 58 ✅ |
| 2017년 제2회 | `2017-2.js` | B | 42 ~ 60 | 391~409 | 57 ✅ |
| 2018년 제2회 | `2018-2.js` | B | 61 ~ 77 | 410~426 | 57 |
| 2019년 제2회 | `2019-2.js` | B | 79 ~ 98 | 427~446 | 59 |
| 2019년 제3회 | `2019-3.js` | B | 99 ~ 119 | 447~467 | ? |
| 2020년 제2회 | `2020-2.js` | C | 0 ~ 18 | 467~485 | ? |
| 2020년 제3회 | `2020-3.js` | C | 19 ~ 39 | 486~506 | ? |
| 2021년 제3회 | `2021-3.js` | C | 40 ~ 60 | 507~527 | ? |
| 2022년 제3회 | `2022-3.js` | C | 61 ~ 80 | 528~547 | ? |
| 2023년 제3회 | `2023-3.js` | C | 81 ~ 99 | 548~566 | ? |
| 2024년 제3회 | `2024-3.js` | C | 100 ~ 123 | 567~590 | ? |
| 2025년 제4회 | `2025-4.js` | C | 124 ~ 144 | 591~611 | ? |

> **쪽 범위는 대략입니다.** 첫 쪽의 회차 제목과 각 쪽 바닥의 정답란을 직접 보고
> 맞는지 확인하세요. 다음 회차 제목이 나오면 거기서 멈춥니다.

### 반드시 알아 둘 것

- **회차마다 60문항이 아닙니다.** 교재 426쪽에 이렇게 적혀 있습니다 —
  *"KEC(한국전기설비규정) 적용 및 관련 법령 개정으로 삭제된 문제가 있어 60문항이 되지 않음을 알려드립니다."*
  빠진 번호는 스캔 누락이 아니라 **교재가 뺀 것**입니다. 번호는 원본 그대로 두세요.
- **같은 쪽을 두 번 찍은 중복본이 있습니다.** 건너뛰세요.
  `pB/078`(=077과 같음) · `pB/113`(=112와 같음) · `pC/050`(=049와 같음)
- 2016년 제5회부터는 CBT라 **기출복원문제**입니다. 회차 제목에 그대로 씁니다.

---

## 3. 쪽 그림 뽑기

세션마다 scratchpad 가 달라서, 스캔 쪽은 **저장소 밖 공용 폴더**에 모읍니다.
이미 뽑혀 있으면 건너뛰므로 여러 번 돌려도 괜찮습니다.

```bash
python pages.py B 61 77
```

→ `C:\Users\user\Desktop\claude code\_scan_신재생\pB\061.png` … 로 저장됩니다.
이 PNG 를 Read 도구로 **한 번에 4쪽씩** 읽으면서 전사하세요.

---

## 4. 문항 적는 법

조각 파일(scratchpad 아무 이름)에 **문항 객체만** 쉼표로 이어 적고 붙입니다.
배열 괄호 `[ ]` 는 쓰지 않습니다.

```js
{ no:12, q:"문제 지문", ch:["보기1","보기2","보기3","보기4"], a:2,
  ex:"해설 본문. 줄바꿈은 \n" },

{ no:13, q:"그림이 있는 문제", img:"img/x2018-2-q13.jpg",
  ch:["…","…","…","…"], a:4, ex:"…", exImg:"img/x2018-2-q13ex.jpg" },
```

```bash
python addq.py 2018-2 조각.js
```

| 키 | 뜻 |
|---|---|
| `no` | 교재의 문항 번호 그대로 |
| `q` | 지문. `[보기]` 상자는 `\n\n［ … ］` 로 본문에 넣는다 |
| `ch` | 보기 4개 (①②③④ 순서 그대로, 번호는 빼고 내용만) |
| `a` | **정답 번호 1~4.** 각 쪽 **바닥의 정답란**에서 읽는다 |
| `ex` | 해설. 없으면 `ex:""` |
| `img` | 문제 그림 |
| `exImg` | 해설 그림·표 |

### 지켜야 할 것

- **보기 앞에 ①②③④ 를 다시 붙이지 마세요.** 화면이 알아서 붙입니다.
- 해설의 수식은 `√` `×` `÷` `≒` `²` 처럼 **문자로** 적습니다. 이미지로 만들지 않습니다.
- 교재 표현을 그대로 옮깁니다. 요약하거나 고쳐 쓰지 않습니다.
- 교재의 오탈자를 발견하면 **고치지 말고** 해설 끝에
  `\n\n※ 교재 원문에는 …로 인쇄되어 있으나 …이다.` 로 적어 둡니다.

---

## 5. 그림·표 잘라내기

원본 스캔을 그대로 씁니다. 좌표는 **쪽 크기에 대한 비율 0~1**입니다.

```bash
python crop.py B 61 0.19 0.21 0.90 0.40 x2018-2-q12ex.jpg
```

- 파일명은 **`x<연도>-<회차>-q<번호>.jpg`**, 해설 그림이면 뒤에 `ex` 를 붙입니다.
  이렇게 하면 세션끼리 파일명이 겹치지 않습니다.
- 자른 뒤 **Read 로 눈으로 확인**하세요. 위에 문제 지문이 걸려 들어오면 `y0` 를 조금 키웁니다.
- 앞 회차와 **똑같은 표**가 나오면 새로 자르지 말고 기존 파일명을 그대로 `exImg` 에 씁니다.

---

## 6. 다 하면

**마지막 회차를 끝낸 세션이 빌드·배포까지 합니다(7번).** 누가 마지막인지는
`ls exam` 로 13개가 다 있는지 보면 압니다. 아직 남아 있으면 빌드하지 말고
남은 회차를 이어서 맡으세요.


**git 은 건드리지 마세요.** 여러분이 만드는 것(`exam/*.js`, `img/*.jpg`)은 전부
`.gitignore` 대상이라 커밋할 것이 없습니다. 빌드와 배포는 마지막에 한 세션이 한 번만 합니다.

끝내기 전에 이것만 확인하세요.

```bash
node -e "const E=require('./exam_book.js'); E.forEach(e=>console.log(e.title, e.qs.length+'문항', '번호 '+e.qs[0].no+'~'+e.qs[e.qs.length-1].no)); console.log('합계', E.reduce((a,e)=>a+e.qs.length,0))"
```

```bash
node -e "
const fs=require('fs');
const e=require('./exam/2018-2.js');
const bad=[];
e.qs.forEach(q=>{
  if(!(q.a>=1&&q.a<=4)) bad.push(q.no+' 정답범위');
  if(!q.ch||q.ch.length!==4) bad.push(q.no+' 보기수');
  if(/^[①②③④]/.test(q.ch[0]||'')) bad.push(q.no+' 보기에 번호가 붙음');
  [q.img,q.exImg].filter(Boolean).forEach(p=>{ if(!fs.existsSync(p)) bad.push(q.no+' 그림없음 '+p); });
});
const nos=e.qs.map(q=>q.no), dup=nos.filter((n,i)=>nos.indexOf(n)!==i);
console.log('이상:', bad.length?bad:'없음', '| 번호중복:', dup.length?dup:'없음');
"
```

---

## 7. 마지막 한 번만 (모든 회차가 끝난 뒤)

```bash
python build_lock.py --pw <교사용 암호>
```

```bash
git add -A && git commit -m "기출 13회차 추가" && git push origin main
```
